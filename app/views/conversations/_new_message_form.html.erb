<%= form_for [conversation, new_message] do |f| %>
        <%= f.hidden_field :user_id, value: current_user.id %>
        <%= f.text_area :body, class: "form-control" %>
        <%= f.submit "Send", class: "btn btn-success" %>
      <% end %>
